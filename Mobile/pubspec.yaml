name: latinoamerica_comparte_admin
description: CMS Admin Móvil — Latinoamérica Comparte
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Estado
  provider: ^6.1.1

  # HTTP
  dio: ^5.4.0

  # Almacenamiento seguro del JWT
  flutter_secure_storage: ^9.0.0

  # Navegación
  go_router: ^13.0.0

  # Íconos
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true